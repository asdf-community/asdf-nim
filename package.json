{
  "devDependencies": {
    "bats": "^1.2.1",
    "bats-assert": "^2.0.0",
    "lintball": "^1.1.3"
  },
  "scripts": {
    "check": "lintball check",
    "fix": "lintball fix",
    "install-tools": "install-tools --yes",
    "test": "bats test --tap"
  }
}
