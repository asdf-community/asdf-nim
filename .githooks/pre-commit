#!/usr/bin/env bash

set -ueo pipefail

# shellcheck source=SCRIPTDIR/../lint/lib/lint_utils.bash
source "$(dirname "${BASH_SOURCE[0]}")/../lint/lib/lint_utils.bash"

while read -r path; do
  lint "$path" "yes"
done <<<"$(git diff --name-only --staged)"
